<?php

it('registers an array of namespaces for autoloading');

it('loads vuex modules using the load method');

it('loads vuex modules lazily using the lazyLoad method');

it('loads data using the deprecated store method with a closure function');

it('loads data into the base vuex state');

it('loads data into vuex modules state');

it('outputs stored data as an array');

it('outputs stored data as a json string');

if('outputs stored data as a (json) http response');

it('converts itself into a json serializable array');

it('can verify any collection/array/object/anything as valid/invalid state');

it('queues actions to be dispatched on the front end');

it('queues mutations to be committed on the front end');
