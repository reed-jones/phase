parameters:
    # for "merge" command
    data_to_append:
        require-dev:
            phpunit/phpunit: '^7.3'
            symplify/monorepo-builder: '^7.3'

    # for "split" command
    directories_to_repositories:
        packages/phase-routing-php: 'git@github.com:reed-jones/phase-routing-php.git'
        packages/phase-state-php: 'git@github.com:your-monorepo/phase-state-php.git'
