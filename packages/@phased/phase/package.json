{
  "name": "@phased/phase",
  "version": "0.0.3",
  "description": "Auto SPA configuration focused on Laravel apps",
  "author": "Reed Jones <reedjones@reedjones.com>",
  "homepage": "",
  "license": "ISC",
  "main": "dist/phase.cjs.js",
  "module": "dist/phase.esm.js",
  "browser": "dist/phase.umd.js",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "compile": "rollup -c",
    "dev": "rollup -c -w",
    "build": "yarn types && NODE_ENV=production yarn compile",
    "test": "jest",
    "pretest": "yarn compile",
    "types": "tsc",
    "check": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@phased/state": "^0.0.3",
    "@phased/routing": "^0.0.3",
    "@phased/laravel-mix": "^0.0.3",
    "@phased/webpack-plugin": "^0.0.3",
    "@phased/types": "^0.0.3"
  },
  "devDependencies": {
    "@phased/types": "^0.0.3"
  }
}
